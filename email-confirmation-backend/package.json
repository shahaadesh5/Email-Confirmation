{
  "name": "email-confirmation-backend",
  "version": "1.0.0",
  "description": "MEAN stack application to send email confirmation on creating an account and allowing user to log in only if email confirmation is done by clicking on the confirmation link received in their email",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aadesh Shah",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "mongoose": "^5.5.2",
    "nodemailer": "^6.1.1",
    "randomstring": "^1.1.5"
  }
}
